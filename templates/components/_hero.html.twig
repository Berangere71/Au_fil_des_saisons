<section class="hero {{ hero_class|default('') }}">

    <div class="hero-overlay">

        <div class="hero-content">
            {% if hero_title|trim %}
                <h1 class="hero-page-title">{{ hero_title|raw }}</h1>
            {% endif %}

        </div>

    </div>

</section>
